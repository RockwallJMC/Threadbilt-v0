import GraphExplorer from 'components/sections/graph-explorer';

export const metadata = {
  title: 'Graph Explorer | PierceDesk',
  description: 'Visualize relationships between accounts, contacts, projects, and more',
};

export default function GraphExplorerPage() {
  return <GraphExplorer />;
}
